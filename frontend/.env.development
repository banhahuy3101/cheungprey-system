# npm run dev — browser calls /api on same origin (no mixed-content).
VITE_API_URL=/api

# Local backend only:
VITE_API_PROXY_TARGET=http://127.0.0.1:8080

# AWS ECS API (uncomment current IP after redeploy):
# VITE_API_PROXY_TARGET=http://18.143.148.194:8080
